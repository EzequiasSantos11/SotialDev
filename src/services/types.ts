export type MessagesTypes = {
  id: number,
  name: string,
  office?: string,
  date: string,
  active: boolean,
  isUser: boolean,
  ImagePeople?: string,
  messageImage?: string,
  messageText: string,
}